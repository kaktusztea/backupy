### BUG ==============================================================
* fullpath + exclude_dir
  → also excludes if matches in full path anywhere    ('temp')
  → exact match needed (dirs[n] == exdir for exdir in bckconfig['exclude_dir_names'])



### TODO =============================================================
* check: ',' at the end of exclude lists cause any problem?

* create_config_file(): store config lines in a fix order


* print elapsed time at the end  

...
* overwrite = yes/no

* compress_zip()
  * Implement "follow symlinks" feature



### FEATURE ==========================================================
* exclude_unique_file1  -> implement

* [META] section
  description = "This is my set for weekly data backup"
  author = myuser
  creation_date = 2016-09-01

* [HOME_CONFIGS] unique section
 * only $HOME/.* files, dirs, no symlink follow, targz fixed, withpath yes, 
   * enabled; result_dir; ..
   * filelista array should be constructed (it slows a little)

* Section class  (is this in configparser?)
  * self-validation, strip, trim at set() time!
  http://stackoverflow.com/questions/4014621/a-python-class-that-acts-like-dict
  https://docs.python.org/3/reference/datamodel.html?emulating-container-types#emulating-container-types

* reserve and move n previous backups

* comment file addition to archive

* filelist file creation from archive - after compression
  
* pre-calculate estimated file size -> predict if free space will be enough or not
  http://stackoverflow.com/questions/12480367/how-to-generate-directory-size-recursively-in-python-like-du-does
  [GLOBAL]
  precalculate_space = yes/no

* logging
  [GLOBAL]
  logging = empty OR file path  ( /home/myuser/log/backupy_heti.log)



### TEST =============================================================
* write testcases
